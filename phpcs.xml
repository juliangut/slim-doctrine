<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="Slim3 Doctrine PHP_CodeSniffer Rule Set">
    <description>Slim3 Doctrine integration PHP_CodeSniffer Rule Set</description>

    <!-- Import PSR2 entire rule set -->
    <rule ref="PSR2"/>

    <!-- exclude files/directories -->
    <exclude-pattern>./build/*</exclude-pattern>
</ruleset>
